import { ArticleLayout } from '@/components/ArticleLayout'
import { CommonLink } from '@/components/CommonLink'

export const meta = {
  author: 'Prakhar Nagpal',
  date: '2023-01-02',
  title: 'Writing a compiler in Python',
  description:
    'I decided to write a language called Monk, complete with all of my favorite things about Python and Rust (two of my favorite languages). It combines indent based scoping and context managers from Python and borrow checking, static typing, and low level systems programming from Rust. The perfect programming language?',
}

export default (props) => <ArticleLayout meta={meta} {...props} />

```python
def evaluate() -> None:
    print("In the evaluation function")

if __name__ == "__main__":
    evaluate()
```
